using Microsoft.VisualStudio.VCProjectEngine;

namespace AsmExplorer
{
    class Compiler
    {
        public static void Invoke(string file, string commandLine)
        {
        }

        #region Command line

        public static string ComposeCommandLine(VCCLCompilerTool cl)
        {
            string cmdLine = null;
            return cmdLine;
        }

        

        #endregion
    }
}
